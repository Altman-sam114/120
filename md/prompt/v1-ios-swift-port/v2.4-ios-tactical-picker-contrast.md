# v2.4 / iOS tactical picker contrast

## 版本

- 版本号：`v2.4`
- 分配依据：Agent A 自动递增；接续 v2.3 遗留的 picker/segmented 对比收紧。

## 目标

1. 让 Speed segmented、Selection mode segmented、Map menu 在深战术 HUD 上可读，不再系统灰洗白。
2. 用 theme token + 共享 View modifier 包装，不改 binding、选项、快捷键、VoiceOver 语义。
3. push `origin/main`，云端 v1.2 visual smoke 验收。

## 非目标

- 不改命令语义、Core、AI、存档、Web、layout metrics。
- 不重写为自定义 segmented 组件库；不跑本机 Xcode/Simulator。

## 实现

1. `TacticalHUDTheme` 增加 picker 背景/描边/前景 token。
2. `TacticalHUDComponents` 增加 `tacticalSegmentedPicker()` / `tacticalMenuPicker()` modifier。
3. 应用到 status speed、dock selection mode、session map。
4. 同步 README / flow / flowchart / test / update_log。

## 验收

- CI v1.2 success；`ios-home.png` 中 segmented/menu 区域对比应高于系统默认灰。
- action 与选项行为不变。
