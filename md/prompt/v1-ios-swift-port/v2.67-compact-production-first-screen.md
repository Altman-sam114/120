# v2.67 Compact production first-screen UX

## 版本背景

- 版本：`v2.67`。
- 最新 Home artifact 显示 compact production section 同时呈现 Production focus summary 与重复的 `FACTORY TECH T2 1.25x MAX` 卡；重复信息挤压首屏，Light Tank 生产卡底部被裁切。
- 本轮只压缩 presentation 重复，不改变生产规则或入口动作。

## 目标

1. 在 compact、非 accessibility Dynamic Type、工厂已达 MAX 且没有升级动作/进度时隐藏重复 Factory Tech 卡；Production focus summary 继续显示 T2、倍率和 MAX。
2. T1 工厂、仍可升级的 T2 READY、升级中状态、regular layout 和 accessibility Dynamic Type 继续显示 Factory Tech 及其 action/progress/cancel 语义。
3. 生产卡顺序、`queueUnit`、Cancel/Repeat/Rally、Shift+1-9、VoiceOver、44pt、Core、存档和 Web 版保持。

## 允许修改范围

- `ios/RustwarIOS/RustwarIOS/TacticalProductionSectionView.swift`
- 如需同步记录：`README.md`、`md/flow/flow.md`、`md/flow/flowchart.md`、`md/test/test.md`、`update_log.md`

## 非目标与云端验证

- 不修改 `GameController`/Core 生产 availability、队列、资源、人口、快捷键或存档字段。
- 不删可执行升级入口；只有 compact MAX 且无 upgrade progress/control 时收起重复 presentation。
- `.wp` 必须保持未跟踪。
- 禁止本地测试、build、Simulator、Preview、浏览器、截图和 `git diff --check`；只提交 `main` 并由最新 `origin/main` 对应 Actions artifact 验收。
- Agent C 必须人工查看最新 `ios-home.png`：Production focus 保留，首排 Scout/Light Tank 卡片不被重复 tech 卡挤压或裁切；`ios-combat.png` 的模型、弹道、爆点、Quick Orders 和 Tactical Map 无回退。静态 PNG 不能证明真实滚动、VoiceOver、Dynamic Type 全档位或真机点击。

