# v2.8 / iOS selection highlight contrast

## 版本

- 版本号：`v2.8`
- 分配依据：Agent A 自动递增；接续 v2.7 后收紧选中高亮可读性。

## 目标

1. 主战场单位 selection ring / 建筑 selection corners 更高对比：双描边、轻微外环、更粗线宽。
2. 战术小地图选中单位/建筑描边更醒目。
3. 不改选择逻辑、多选集合、命令语义。
4. 云端 v1.2 visual smoke 验收。

## 非目标

- 不改 Core、AI、存档、Web。
- 不跑本机 Xcode/Simulator。

## 实现

1. `BattlefieldScene.addSelectionRing` / `addSelectionCorners` 增强对比。
2. `TacticalMapView` selected unit/building stroke 增强。
3. 文档同步。

## 验收

- CI success；相关文件进入 arm64/x86_64 编译。
- 选择语义不变。
