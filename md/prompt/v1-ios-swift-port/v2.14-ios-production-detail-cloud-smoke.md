# v2.14 iOS Production Detail And Cloud Smoke

## 目标

本轮由主线程直接执行，不调用子 Agent。精修原生 iOS Production section 的信息密度，并让云端唯一 visual smoke 预选己方 Land Factory，从 artifact PNG 直接覆盖 v2.13 的建筑生产首屏。

## 实现要求

### 专用云端启动态

- 保留既有 `--rustwar-ci-visual-smoke` 参数和 CI schema v1.2。
- 参数存在时仍暂停战局，并通过正常 Core selection API 预选第一座完成状态己方 Land Factory。
- 普通启动无预选、默认运行，行为不得改变。
- `GameController` initializer 接收可选初始己方建筑类型，默认 nil；不得直接写 selected ids 绕过 Core。

### Production UI

- 每个单位生产按钮同时显示：可辨识 SF Symbol、单位名、Metal cost、supply 和 build time。
- 费用/人口/时间使用图标和文字，不只靠颜色；支持 Dynamic Type 和 1/2 列布局，不使用固定字体大小。
- Button action、Shift+1-9 快捷键、44pt 最小触控、queue result feedback 保持。
- 当前队首显示 Queue label、单位/百分比和 `ProgressView`；VoiceOver label/value 保持完整。
- 不新增 sprite、Core 字段、单位、生产规则或第二套 queue 状态。

## 云端验收

- 只认最新 `origin/main` 精确 SHA 的 `Rustwar CI Results` run 和 v1.2 artifact。
- JUnit 8 checks、0 failures、1 browser skip；固定 Xcode 26.5、iOS 26.5 Simulator、303 Core tests、双架构 build、launch、PNG、ImageIO probe 全部成功。
- build log 证明 `RustwarIOSApp.swift`、`GameController.swift`、`TacticalProductionSectionView.swift` 双架构编译。
- manifest 的 `visualSmokeLaunchArgument` 仍为 `--rustwar-ci-visual-smoke`。
- Agent C 必须人工查看 PNG：Selection 显示 Land Factory，Production 位于 Commands 前，至少可见生产单位按钮及费用/人口/时间，不与战场、状态栏或 dock 重叠。
- screenshot 仍不证明真实 tap、scroll、生产 action 或 VoiceOver；这些限制必须保留。

## 文档、提交与禁令

更新 README、flow、flowchart、test、update log，并回填 v2.13 run `29225049932` / artifact `rustwar-ci-v1.2-main-33ceaa9-run29225049932-attempt1` 的通过证据。

禁止全部本地验证，包括 `git diff --check`、Node/Swift check、Swift tests、Xcode、Simulator、Preview、截图和浏览器。只做状态/范围核对，提交 `v2.14: refine production cloud smoke`，直接 push `origin/main`，下载精确 artifact 到 `/private/tmp/rustwar-c-review-<run_id>/` 复判。不得暂存用户的 `project.pbxproj` 或 `.wp`。
