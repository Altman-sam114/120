# v2.7 / iOS tactical map pending chrome

## 版本

- 版本号：`v2.7`
- 分配依据：Agent A 自动递增；接续 v2.6 后收紧战术小地图等待命令 chrome。

## 目标

1. 等待目标命令时，Tactical Map 外框、pending badge 使用 theme attention/control tokens，不再依赖系统灰/薄 material 与硬编码黑胶囊。
2. 不改 pending label/symbol 逻辑、手势、命令派发。
3. 云端 v1.2 visual smoke 验收。

## 非目标

- 不改 Core、AI、存档、Web、layout metrics。
- 不跑本机 Xcode/Simulator。

## 实现

1. `TacticalHUDTheme` 增加 map chrome/pending badge tokens。
2. `TacticalMapView` 背景、边框、pending badge 消费 theme。
3. 文档同步。

## 验收

- CI success；`TacticalMapView` 进入 arm64/x86_64 编译。
- 默认首屏通常无 pending，waiting chrome 靠代码路径验收。
