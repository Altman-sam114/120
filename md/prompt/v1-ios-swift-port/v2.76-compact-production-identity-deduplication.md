# v2.76 Compact production identity deduplication

## 版本与轮次

- 版本：`v2.76`
- 总目标 X：持续把原生 iOS RTS 的操作、UI、模型和战斗视觉迭代到比 Rusted Warfare 更直接、更清晰且更精细。
- 当前轮次：删除 compact 普通 Dynamic Type 下 Production header 与 focus summary 的重复建筑身份行，让第二排 Arty / AA / Heavy 更早进入首屏，同时保留完整管理状态、可访问性和 Core 生产语义。
- 上一轮基线：v2.75 最终文档 commit `2211873070027eea59b0ea424e40ae351e827f90` 对应 run `32644299678` / attempt `1` / job `97206042010` 的 artifact 已下载复判；Home SHA-256 为 `f2238e3bfb7918b0db80f2cda7d97533c670db51e7c91c358a3a24acb72a1bcf`，Combat 为 `01fcba16813a13a14b93dcbd78f13573ae2ab29a98327768af91af5bda0bab9e`。旧 artifact 不得作为 v2.76 实现证据。

## 公开参考与问题

Steam 官方 Rusted Warfare 页面强调大规模战斗、无限缩放和跨全战场下令；这要求固定 HUD 在不牺牲可读性和触控面积的前提下尽量减少重复信息。媒体资源受用户既有浏览安全偏好限制，本轮不绕过访问；布局判断以官方页面公开说明和 Rustwar 自身最新云端 Home PNG 为证据。

v2.75 Home fixture 使用约 210pt 的 compact trailing dock。固定 `TacticalCompactProducerHeaderView` 已显示 `Production / T2 / 1.25x / Land Factory`，下面的 `TacticalProductionFocusSummaryView.identitySummary` 又重复 `Land Factory / T2 / 1.25x`。重复行把第二排 Arty / AA / Heavy 推到首屏底边，只露出卡片顶部。

## 目标

1. 仅在 `isCompact && !dynamicTypeSize.isAccessibilitySize` 时省略 focus summary 的可见 identity row。
2. Compact summary 的 NOW / QUEUE / UPGRADE strip 必须保留；完整 `summaryAccessibilityValue` 继续朗读建筑名、tech、速度、Now、Queue、Build 与 Upgrade。
3. Regular trailing 和所有 accessibility Dynamic Type 路径继续显示完整 identity title/metrics；没有 compact producer header 时不得丢失建筑身份。
4. 不缩小字体、卡片、图标、间距或 44pt 控件，不改变 Scout / Light / Hover / Arty / AA / Heavy 顺序；只通过删除重复层级让第二排更早可见。
5. 保持 management rail 紧随 Production 标题，Factory Tech、production options、queue、Cancel、Repeat、Rally 和 VoiceOver 语义不变。

## 允许修改范围

- `ios/RustwarIOS/RustwarIOS/TacticalProductionSectionView.swift`
- `README.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `md/test/test.md`
- `md/prompt/v1-ios-swift-port/v2.76-compact-production-identity-deduplication.md`
- `update_log.md`

## 实现要求

### Summary visual gate

- `TacticalProductionFocusSummaryView.body` 继续使用一个 summary card；不能隐藏整个 summary。
- `identitySummary` 只在 `!isCompact || dynamicTypeSize.isAccessibilitySize` 时进入可见层级。
- `focusRows` 必须无条件保留；compact 继续使用 `ViewThatFits` 的 NOW / QUEUE / UPGRADE strip 与 full rows fallback。
- 不修改 `identitySummary`、`identityTitle`、`identityMetrics` 或 `summaryAccessibilityValue` 内容，确保 regular/accessibility 和 VoiceOver 保持完整。

### 保持项

- `TacticalCompactProducerHeaderView` 继续显示 Production、building name、tech 与 speed，并保留独立 VoiceOver label/value/hint。
- `TacticalProductionManagementRail` 的 Cancel / Repeat / Rally 顺序、44pt、disabled/active 状态和菜单行为不变。
- `TacticalFactoryTechView` 的 compact T2/MAX 抑制、regular/accessibility 完整展示、升级操作和状态不变。
- `TacticalProductionButtonLabel`、三列网格、单位顺序、成本/supply/time、disabled reason、快捷键、Queue view 与退款不变。
- 不改 `GameController`、RustwarCore、AI、production queue/repeat/retry、rally、命令、战斗、触控、Tactical Map、存档/JSON 或 Web 版。
- `.wp` 保持未跟踪，不得编辑、暂存或提交。

## 非目标与禁止项

- 不缩小 command dock、生产卡、字体、图标或 44pt 触控目标；不移动 management rail 到别处。
- 不重构整个 920 行 Production 文件，不拆新类型，不改 HUD layout breakpoints 或 fixture。
- 不修改 v2.75 impact、模型、武器 profile、Core 数值、workflow 或截图验证脚本。
- 本轮禁止本机运行 SwiftPM test/typecheck、Swift parse/typecheck、Xcode build/list、Simulator、Preview、截图、Node、浏览器 smoke、测试脚本和 `git diff --check`。

## Agent B Git 交付

1. 在 `main` 确认与 `origin/main` 同步、活动 GitHub 账号为 `Altman-sam114`，工作区只有本轮允许文件与未跟踪 `.wp`。
2. 只读取普通 `git status`、`git diff`、`git diff --stat` 和提交范围；不得把这些写成测试通过。
3. 只暂存本轮七个允许文件，建议提交信息：`feat(ios): v2.76 surface production second row`。
4. push 前再次确认分支、远端、账号与 staged 范围；直接 push 到 `origin/main` 触发 Actions，不建分支或 PR。

## 云端唯一验证与 Agent C 验收

Agent C 只验收 v2.76 最新 `origin/main` 完整 SHA 对应的 `Rustwar CI Results` run，并把唯一必要未加密 artifact 下载到 `/private/tmp/rustwar-c-review-<run_id>/`。必须核对：

- manifest 的 `branch=main`、完整 `commitSha`、run id/attempt、schema、Xcode 26.5、iOS 26.5、Swift 6.3.2 和固定 iPhone 17 Pro；
- JUnit、主日志、失败摘要、repo state；唯一允许 skip 是既有 headless browser regression；
- Swift Core 至少 342 tests、Xcode list/build、arm64/x86_64 iOS build、production/combat 双启动、横屏归一化与双 PNG probe 全成功；
- `TacticalProductionSectionView.swift` 进入双架构编译；源码复判确认 compact normal 唯一 gate、regular/accessibility identity、VoiceOver value、focus rows、44pt 和行为保持项。

Home PNG 必须改变，人工与 v2.75 `f2238e3b...` 对比：

- 固定 header 仍清楚显示 Production / T2 / 1.25x / Land Factory；
- summary 不再重复 Land Factory / T2 / 1.25x，NOW / QUEUE / UPGRADE 完整；
- Cancel / Repeat Off / Rally 和 Scout / Light / Hover 第一排不变；
- Arty / AA / Heavy 第二排至少图标与单位名进入首屏，且无重叠、压缩、截字或 44pt 回退。

Combat PNG 应与 v2.75 SHA-256 `01fcba16813a13a14b93dcbd78f13573ae2ab29a98327768af91af5bda0bab9e` 逐字节一致；变化必须人工解释并检查 v2.72 武器层级、v2.75 impact、模型、Quick Orders、Tactical Map 与状态栏。

## 证据边界与停止条件

- 固定 Home PNG 只覆盖 compact trailing 普通 Dynamic Type；regular、compact bottom、accessibility Dynamic Type、VoiceOver、滚动和真实点击主要依赖源码合同与双架构编译，不能宣称已完成真机交互验证。
- 静态首屏可证明第二排位置变化，不能证明所有语言、所有屏幕尺寸或超长队列布局。
- 总目标 X 不因 v2.76 单轮通过而完成；通过后继续触控、模型与战斗视觉精细化轮次。
