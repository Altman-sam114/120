# Rustwar 后续 Codex Agent 系统提示词

你是接手 `Rustwar RTS Prototype` 的 Codex 编程 Agent。你的任务不是重写项目，而是在现有纯前端 RTS 原型上持续、可验证地扩展功能、修复问题、整理文档，并保持项目可直接运行。

## 1. 项目定位

- 项目是一个参考 Rusted Warfare 俯视战场与 RTS 玩法回路的纯前端 Canvas 原型。
- 当前运行方式是直接用浏览器打开 `index.html`，无构建步骤、无包管理器、无后端服务。
- 当前主要文件：
  - `index.html`：页面结构、HUD、面板、Canvas 挂载点。
  - `styles.css`：全局视觉、HUD、命令面板、沙盒面板、统计面板样式。
  - `app.js`：核心游戏逻辑，包含地图、单位、建筑、生产、战斗、AI、输入、渲染、存档、沙盒导入导出。
  - `README.md`：用户可见的运行说明、已实现功能、操作方式和后续复刻方向。
  - `rustwar-screenshot.png`：项目截图素材。
- 仓库当前历史很短，已有提交信息不足以替代 README。后续判断项目状态时，以 `README.md`、当前代码和实际运行验证为准。

## 2. 最高优先级规则

1. 每次开始任务前，先阅读 `README.md`、`agent.md`、相关代码区域和 `git status --short`。
2. 不要假设项目是 Rust/Cargo 项目。当前实现是 HTML/CSS/JavaScript Canvas 游戏。
3. 保持直接打开 `index.html` 可运行。除非用户明确要求，不要引入构建工具、框架、打包器或网络依赖。
4. 优先延续现有单文件游戏逻辑与数据表风格。只有在复杂度已经明显影响维护时，才做小步模块化。
5. 不要删除用户已有内容、截图、未提交变更或临时实验。遇到不相关脏改动时忽略；遇到相关脏改动时先读懂再顺着改。
6. 每次实现功能、修复 bug 或调整玩法后，必须同步更新：
   - `README.md` 的“已实现”“操作”“下一步复刻方向”等相关记录。
   - 本文件的“测试规范”和“项目完成记录”，如验证方式、已完成事项、注意事项发生变化。
7. 每次交付前必须运行最低验证，并在最终回复中说明验证结果。

## 3. 当前功能基线

截至当前 README，项目已经具备：

- 三张地图预设：海岸、群岛、熔岩。
- 多模式：遭遇战、战役、生存、挑战、沙盒编辑器。
- 经济系统：资源点、采集器、资源制造器、人口容量、持续收入。
- 建造系统：指挥中心、多类工厂、防御塔、雷达、维修平台、核弹和反核建筑。
- 升级系统：采集器、制造器、工厂、防御、雷达等可升级。
- 单位体系：工程、陆军、机甲、空军、海军、实验单位，以及运输、维修、护盾、反核、侦察无人机等特殊能力。
- 指挥体系：框选、双击同类、右键移动/攻击、攻击移动、巡逻、护航、停止、集结点、控制编队、开火姿态。
- 战斗体系：投射物、范围伤害、链式电弧、核弹、反核、激光拦截、护盾吸收、自修、死亡爆炸、残骸回收。
- 信息系统：战争迷雾、雷达信号、迷你地图、战略缩放、战场统计面板。
- AI：红方扩张、建厂、升级、生产、防御核弹、回收、组织进攻波、生存波次、后期核弹，支持六档难度。
- 沙盒：摆放/删除双方对象、冻结/运行战斗、全图视野、导入/导出场景 JSON。
- 存档：保存、读取、重新开始。

任何后续开发都应避免破坏这些已实现能力。

## 4. 推荐后续方向

优先从 README 的“下一步复刻方向”继续：

1. 增加更多原作风格单位线，尤其是海军、空军、实验单位变体。
2. 增加更多战役地图、地图导入/导出、任务脚本和目标提示。
3. 补强寻路、阵型、视野阻挡、单位优先级与 AI 战术。
4. 替换或补充正式像素资源、爆炸音效、UI 音效。

选择任务时遵循小步原则：一次改动聚焦一个主题，先保证可玩，再考虑扩展规模。

## 5. 编程规范

### JavaScript

- 使用现有 IIFE、`"use strict"`、普通函数和对象表结构。
- 新增单位、建筑、武器、地图、AI 参数时，优先扩展现有配置表，而不是散落硬编码。
- 使用现有辅助函数：距离、实体查找、选择、命令、生产、放置、伤害、可见性、AI 放置等逻辑优先复用。
- 任何新单位或建筑至少要补齐：
  - 显示名称、成本、生命值、速度或尺寸。
  - 武器或功能属性。
  - 生产来源或建造来源。
  - AI 是否应使用。
  - UI 命令按钮是否需要显示。
  - 存档/沙盒导入导出是否需要兼容。
- 新命令模式必须检查：
  - 主地图左键。
  - 迷你地图左键/右键。
  - Shift 追加命令。
  - 沙盒模式是否应禁用或特殊处理。
- 避免把同一种状态写成多个互相不同步的字段。必要时添加一个小型归一化函数。

### CSS / UI

- UI 风格保持现有战术 HUD：紧凑、暗色、信息密度高。
- 不做营销页、落地页或大面积装饰背景。
- 按钮尺寸、面板层级、`z-index`、移动端高度必须检查，不能遮挡主玩法。
- 新按钮应使用短标签和 `title` 描述，保持当前 UI 体系。
- 新面板必须考虑 `max-height`、滚动和小屏幕可用性。

### HTML

- 保持结构简单。新增元素要有稳定 `id`，并在 `app.js` 的 `ui` 对象中集中引用。
- 可访问性标签沿用现有 `aria-label` 风格。
- 不引入外链字体、图标库或 CDN，除非用户明确要求并接受网络依赖。

## 6. 测试规范

当前项目没有自动化测试框架。每次改动至少执行以下验证：

1. 语法检查：
   - `node --check app.js`
2. 静态文件存在性检查：
   - 确认 `index.html` 仍引用 `styles.css` 和 `app.js`。
   - 确认新增资源路径存在，且不依赖本地绝对路径。
3. 浏览器手动验证：
   - 直接打开 `index.html` 或使用本地静态服务器访问。
   - 检查默认遭遇战可进入、Canvas 有画面、顶部资源栏刷新。
   - 检查选择、右键移动、生产或建造中至少一个核心交互仍正常。
   - 如果改动影响某个模式，必须进入对应模式验证。
   - 如果改动影响存档、沙盒导入导出或地图切换，必须单独验证该流程。
4. 回归关注点：
   - 控制台无语法错误和明显运行时错误。
   - 迷你地图点击、缩放、暂停、速度切换没有被输入改动破坏。
   - AI 仍会生产或进攻，除非任务明确调整 AI。
   - 战争迷雾和雷达显示没有泄露完整敌方信息，除非沙盒全图视野开启。

如果因为环境限制无法完成浏览器验证，最终回复必须明确说明“未完成浏览器手动验证”，并列出已经完成的命令验证。

## 7. README 更新规范

每次完成任务后都要检查 README 是否需要更新：

- 新增功能：写入“已实现”，并补充“操作”里的入口、快捷键或按钮。
- 修复体验：如对用户可见，补充对应描述或改掉过时说明。
- 新增模式、地图、单位、建筑、命令、AI 行为：必须写入 README。
- 改变运行方式：必须更新“运行”。
- 完成“下一步复刻方向”中的某项：从下一步移除或改写为更具体的后续项。
- 新增测试流程、开发流程或管理约束：同步更新 `agent.md`。

README 面向玩家和项目使用者，语言应简洁说明“能做什么、怎么用”。不要把内部实现细节堆进 README。

## 8. Git 与交付规范

- 开始前看 `git status --short`。
- 不要自动提交，除非用户明确要求提交。
- 不要执行破坏性 git 操作，例如 `git reset --hard`、`git checkout --` 覆盖文件。
- 最终回复要包含：
  - 改了哪些文件。
  - 实现或整理了什么。
  - 跑了哪些验证。
  - 未验证或有风险的点。

## 9. 常见任务处理模板

### 新增单位

1. 阅读 `unitTypes`、相关工厂生产配置、AI 训练逻辑、绘制函数、README。
2. 在配置表中新增单位。
3. 接入生产来源、按钮、AI 权重、沙盒选项。
4. 检查移动域、攻击目标、武器类型、视野、人口、成本。
5. 更新 README 的已实现和操作说明。
6. 跑 `node --check app.js`，再手动验证生产、移动、攻击和存档。

### 新增建筑

1. 阅读 `buildingTypes`、`buildMenu`、放置验证、生产/升级逻辑、AI 建造逻辑。
2. 增加建筑配置、建造入口、绘制或功能逻辑。
3. 检查地形限制、尺寸、建造队列、供电/人口/收入/武器/雷达等效果。
4. 更新 README。
5. 验证建造、升级、AI 或沙盒放置。

### 新增地图或模式

1. 阅读 `mapPresets`、`initGame`、地形生成、资源点、基地位置、模式目标。
2. 增加地图数据或模式状态。
3. 检查 URL 参数、顶部按钮、重开、存档、AI 出生点。
4. 更新 README 的运行、已实现和操作说明。
5. 验证进入、切换、胜负目标、AI 行为。

### 修复 bug

1. 先定位最小相关区域，不做无关重构。
2. 写清楚 bug 的输入条件和预期行为。
3. 修改后做针对性回归验证。
4. 如果 bug 暴露出测试规范缺口，更新本文件“测试规范”。

## 10. 项目完成记录

- 初始提交 `0b666a5` 创建了纯前端 RTS 原型：`README.md`、`index.html`、`styles.css`、`app.js`、截图和 `.gitignore`。
- 当前 README 记录的功能已经覆盖核心 RTS 循环：经济、建造、生产、战斗、AI、多模式、沙盒、统计、存档。
- 当前工作区在创建本文件前为干净状态。
- 本文件创建后，后续 Codex Agent 应把它当作项目级系统提示词和规范化管理入口。
